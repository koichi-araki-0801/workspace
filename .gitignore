node_modules/
dist/
*.log
.DS_Store
.env
.env.local
# Local runtime config (copy from appconfig.example.json); may hold machine-specific paths
appconfig.json
coverage/
.vite/
.tmp/
tmp/
*.tsbuildinfo
# Python バイトコードキャッシュ（コミット対象外）
__pycache__/
*.pyc
# Claude Code: マシン固有のローカル設定（auto-push フック等）。共有しない
.claude/settings.local.json
# vivliostyle / playwright artifacts
playwright-report/
test-results/
# pnpm

# オフラインバンドル生成物（コミット対象外。重量物は GitHub Releases へ）
.pnpm-store/
pnpm.tgz
ms-playwright/
# Python ビルド依存の wheel（pip download で生成し重量物バンドルへ同梱）
python-wheelhouse/
*.tar.gz
*.tar.gz.part-*
# Release 公開時の付随生成物
offline-deps-bundle.tar.gz.sha256
bundle.key
_bundle_stage/
_bundle_verify/
# オフライン環境構築後、ダウンロード物（ソースZIP/バンドル/sha256/key）の退避先
bk/
# Claude Code: 計画文書（内部作業用。共有しない）
.claude/plans/
