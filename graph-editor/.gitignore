node_modules/

# desktop app (PyInstaller) build artifacts
build/
dist/
# isolated build venv (created by scripts/build.bat to keep global packages out of the exe)
.venv-build/
__pycache__/
*.spec
# Bundled WebView2 Fixed Version runtime (~180MB, fetched per build host)
webview2_runtime/

# runtime startup diagnostics log (data/startup.log)
data/

# Playwright (E2E) artifacts
test-results/
playwright-report/
.last-run.json
