[project]
name = "pdftosvg"
version = "0.1.0"
description = "PDF を SVG 化し、表ヘッダの用語統一(辞書)と不要箇所のトリミングを非エンジニアでも行えるデスクトップツール"
requires-python = ">=3.10"
dependencies = [
    "PyMuPDF>=1.24",
    "Pillow>=10.0",
    "fonttools>=4.50",
    "brotli>=1.1",
]
# UI は OS 標準の Edge をアプリモードで開く方式 (web.server + resources/web) のため
# PySide6 / QtWebEngine への依存は撤廃した (配布物が大幅に小さくなる)。

[project.optional-dependencies]
dev = ["pytest>=8.0", "pyinstaller>=6.20"]

[project.scripts]
pdftosvg = "app:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
